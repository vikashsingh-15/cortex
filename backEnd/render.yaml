services:
  - type: web
    name: cortex-api
    runtime: node
    rootDir: backEnd
    buildCommand: npm ci --include=dev && NODE_OPTIONS=--max-old-space-size=2048 npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
